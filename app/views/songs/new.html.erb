<h1>Hello World</h1>

<%= form_for [@chart, @song] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, required: true %>
  <%= f.submit 'Add Song', class: 'ui inverted button green'%>
<% end %>